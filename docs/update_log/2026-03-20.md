# 2026-03-20

## Summary
Fixed stale MessageHub key purge running in wrong hook (`before_run` → `before_train`) so flat `mpjpe`/`mpjpe_abs` terminal scalars are actually removed on checkpoint resume.

## Changes

### `pose/mmpose/engine/hooks/train_mpjpe_hook.py`
- **Stale key purge hook fix:** Renamed `before_run` → `before_train`. `before_run` fires before `load_or_resume()`, so the checkpoint would restore the stale keys after the purge. `before_train` fires after `load_or_resume()` inside `train_loop.run()`, so the purge sticks.
