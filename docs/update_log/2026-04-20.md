# 2026-04-20

- `pose/tools/panoptic/panoptic_body3d.py` — Load CMU `body3DScene_*.json` (COCO-19), project world joints (cm) to Kinect RGB with `pose_project_2d`, optional undistort via `cv2.undistortPoints`, draw skeleton overlays.
- `pose/tools/panoptic/preprocess_panoptic_kinect1.py` — `--gt-pose`, `--pose-subdir`, `--pose-conf-thr`; writes `*_pose_gt_bgr.jpg` / `*_pose_gt_undistort.jpg` when pose JSON exists; `gt_pose` field in frame `*_meta.json`. Refactored undistort to expose `new_k` for consistent 2D reprojection.
